[phases.setup]
nixPkgs = ['python311', 'poetry']

[phases.install]
cmds = ['poetry config virtualenvs.create false', 'poetry install --only=main']

[start]
cmd = 'poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT'
